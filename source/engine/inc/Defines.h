#ifndef DEFINES_H
#define DEFINES_H

// This file is getting included in glsl shader so keep it glsl compatible

//#include <stdint.h>
//namespace Common
//{
    // For now change this manually based on the model being loaded
    const int MAX_ENTITIES = 1000;
    const int MAX_MESH_VIEWS_PER_MESH = 10;
//}

#endif